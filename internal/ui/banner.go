package ui

import (
	"fmt"

	"github.com/fatih/color"
)

func ShowStartupBanner(version string) {
	c1 := color.New(color.FgHiCyan, color.Bold).SprintFunc()
	cVer := color.New(color.FgHiBlack).SprintFunc()

	banner := []string{
		`  ██████╗██╗   ██╗██████╗ ███████╗    ██████╗ ██████╗ ██████╗ ███████╗`,
		` ██╔════╝██║   ██║██╔══██╗██╔════╝    ██╔════╝██╔═══██╗██╔══██╗██╔════╝`,
		` ██║     ██║   ██║██████╔╝█████╗      ██║     ██║   ██║██║  ██║█████╗  `,
		` ██║     ██║   ██║██╔══██╗██╔══╝      ██║     ██║   ██║██║  ██║██╔══╝  `,
		` ╚██████╗╚██████╔╝██║  ██║███████╗    ╚██████╗╚██████╔╝██████╔╝███████╗`,
		`  ╚═════╝ ╚═════╝ ╚═╝  ╚═╝╚══════╝     ╚═════╝ ╚═════╝ ╚═════╝ ╚══════╝`,
	}

	fmt.Println()
	for _, line := range banner {
		fmt.Println(c1(line))
	}
	fmt.Printf("\n  %s %s\n", cVer("v"+version), cVer("| AI Coding Agent by bromanprjkt"))
	fmt.Println()
}

func PrintHeader(title string) {
	cTitle := color.New(color.FgHiCyan, color.Bold).SprintFunc()
	cLine := color.New(color.FgHiBlack).SprintFunc()

	fmt.Println()
	fmt.Printf("  %s\n", cTitle(title))
	fmt.Println(cLine("  ─────────────────────────────────"))
	fmt.Println()
}
